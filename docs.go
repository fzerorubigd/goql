// Package goql is a go sql driver for query against go packages
// the tables and fields are registered dynamically at runtime, and registration can be done using Register* functions
// currently 3 types are accepted, Number/String/Bool type `Definition` is under development
// see the cmd/goql for an example.
package goql
